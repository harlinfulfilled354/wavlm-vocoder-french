# Experiment: Baseline without GAN
# Inherits from base.yaml

_base_: ../base.yaml

experiment:
  name: "no_gan_baseline"
  description: "Baseline with spectral losses only (L1 + Multi-Scale STFT)"

loss:
  use_gan: false
  l1_weight: 2.0
  stft_weight: 1.0

training:
  output_dir: "./outputs/experiments/no_gan"
